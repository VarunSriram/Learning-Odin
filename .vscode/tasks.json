{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Odin (Debug)",
            "type": "shell",
            "command": "odin",
            "args": [
                "build",
                "${fileDirname}", 
                "-debug",
                "-out:${fileDirname}/main.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}